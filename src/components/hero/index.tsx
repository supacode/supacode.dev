import React from 'react';

import './hero.scss';

export interface HeroProps {}

export const Hero: React.SFC<HeroProps> = () => {
  return (
    <div className="hero">
      <></>
    </div>
  );
};
